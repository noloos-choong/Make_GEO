# Config (환경 설정·상수)

환경변수, 상수, 전역 설정 파일을 관리합니다.

## 파일 구성 (예시)

- `constants.js` - GEO 점수 가중치, 엔진 목록 등 상수
- `env.js` - 환경변수 로드 및 검증
- `routes.js` - 화면 라우팅 경로 정의
- `api.config.js` - API 엔드포인트, 타임아웃 설정
